var s = '';
for (var i = 0; i < 200; i++) {
    if (i > 0) {
        s += ', ';
    }
    s += Math.trunc(Math.random() * 1000000000000);
}
console.log(s);
document.getElementById('serials').innerHTML = s;

